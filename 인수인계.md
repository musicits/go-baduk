# 바둑 프로젝트 — 현재 상태 정리

> 새 세션에서 이어서 작업할 때 이 파일을 그대로 붙여넣으면 됩니다.
> 마지막 갱신: 2026-08-27 새벽

---

## 한 줄 요약

파이썬으로 만든 바둑 게임. **웹 버전은 완성되어 돌아가고 있고**,
**로컬에서 KataGo 붙이는 것만 남았습니다.**

---

## 지금 상태

| 항목 | 상태 |
| --- | --- |
| 저장소 | https://github.com/musicits/go-baduk (공개) |
| 웹 | https://musicits.github.io/go-baduk/ — **정상 작동** |
| 로컬 폴더 | `D:\Claude Code (2026)\6. 바둑` |
| KataGo 설치 위치 | `D:\Claude Code (2026)\katago\` (예정) |
| 파이썬 | **설치됨, 정상 작동 확인** (`python baduk.py web` 으로 서버 뜸) |
| KataGo | **아직 안 붙음** ← 남은 일 |
| 테스트 | 98개 전부 통과 |

---

## 만들어진 것

### 규칙 엔진 (`baduk/board.py`)
착수, 따내기, 자살수 금지, 단수패, 동형반복 금지(positional superko),
중국식·한국식 계가, 2~9점 접바둑, 9·13·19로.
**일본식은 사용자 요청으로 뺐습니다.**

### KataGo 연결 (`baduk/gtp.py`, `baduk/engine.py`)
GTP 프로토콜로 붙기 때문에 KataGo·Leela Zero·GNU Go 가 모두 됩니다.
`kata-analyze` 를 파싱해 승률·예상집·추천수를 뽑습니다.

KataGo 자동 탐색 순서:
1. `katago경로.txt` 에 적힌 경로 (설치기가 남김)
2. 프로젝트 폴더 옆 `katago/`
3. `KATAGO_PATH` 환경변수, PATH, `~/.katago/` 등

### 화면
- `baduk/cli.py` — 터미널 대국
- `baduk/server.py` — 로컬 웹서버 (127.0.0.1 전용)
- `docs/index.html` — 바둑판 화면. **로컬 서버와 GitHub Pages 가 같은 파일을 씁니다.**
  열릴 때 파이썬 서버가 있는지 보고, 있으면 서버(=KataGo)를, 없으면
  브라우저 안에서 혼자 돕니다.

### 웹 전용 (`docs/rules.js`, `docs/bot.js`)
규칙을 자바스크립트로 다시 구현한 것과, 몬테카를로 탐색(MCTS) 봇.
서버 없이 브라우저만으로 대국이 됩니다.

### 기타
- `baduk/sgf.py` — SGF 기보 저장·불러오기
- `.github/workflows/pages.yml` — main 에 push 하면 자동으로 웹 배포
- 배치 파일들 (아래 참고)

---

## 남은 일 — KataGo 붙이기 (최우선)

### 지금까지 상황

사용자가 KataGo 를 받아서 실행했더니 이 에러가 났습니다.

```
baduk.gtp.GtpError: 명령을 보내지 못했습니다: [Errno 22] Invalid argument
```

**원인:** KataGo 가 시작하자마자 죽었는데, 왜 죽었는지 코드가 안 보여줬습니다.
가장 유력한 원인은 **신경망(.bin.gz) 또는 설정(.cfg) 파일이 없는 것**입니다.
KataGo 는 이때 조용히 종료합니다.

### 이미 고쳐놓은 것 (커밋 `65dff72`)

- 엔진이 stderr 에 남긴 말을 예외 메시지에 붙이도록 수정
- 신경망·설정이 없으면 아예 안 띄우고 "뭐가 없는지" 알려주도록 수정
- 서버가 엔진 오류에도 죽지 않고 내장 봇으로 물러나도록 수정

### 다음 세션에서 할 일

1. 사용자에게 **`git pull`** 하게 한다 (위 수정본을 받아야 함)
2. **`상태 확인.bat`** 을 눌러보게 한다 → KataGo 경로·설정·신경망이 각각
   잡히는지 나옴
3. 없다고 나오는 것을 채운다:
   - `katago.exe` — https://github.com/lightvector/KataGo/releases
     (`opencl-windows-x64` 판. 안 되면 `eigen` = CPU 판)
   - `.bin.gz` 신경망 — https://katagotraining.org/networks/
   - `.cfg` 설정 — KataGo 압축 안에 `default_gtp.cfg` 로 들어 있음
   - **셋을 같은 폴더에** 넣어야 함
4. `python baduk.py engines` 로 확인
5. 잘 되면 첫 수가 **3~10분** 걸린다는 걸 미리 알려준다 (그래픽카드 튜닝,
   최초 1회만)

### 사용자 실력에 맞추기

**사용자는 아마 초단입니다.** KataGo 기본 설정은 프로도 못 이깁니다.
반드시 약화시켜서 써야 합니다.

```
python baduk.py --탐색수 20      # 아주 약하게
python baduk.py --탐색수 200     # 적당히
python baduk.py --접바둑 4        # 4점 접바둑
```

---

## 웹 버전의 한계 (사용자가 여러 번 지적한 부분)

**웹 봇은 9로에서만 쓸 만합니다.**

| 판 | 3초 탐색 시 후보 한 자리당 검토 횟수 | 평가 |
| --- | --- | --- |
| 9로 | 약 440회 | 쓸 만함 |
| 19로 | 약 20회 | 사실상 아무렇게나 둠 |

신경망 없이 규칙만으로 하는 탐색의 한계입니다. 탐색 후보를 돌 주변으로
좁히고 플레이아웃도 국지적으로 바꿔서 개선했지만, **19로는 이 방식으로
불가능합니다.**

### 더 세게 만들려면 (사용자가 물어본 것)

**ONNX Runtime Web / TensorFlow.js + 작은 KataGo 신경망(b6·b10급)** 을
브라우저에서 WebGPU 로 돌리고 그 위에 지금 MCTS 를 얹는 방법.
유단자 상대가 가능해집니다.

대가:
- 신경망 파일 10~50MB (첫 방문 시 다운로드)
- WebGPU 지원 브라우저 필요
- 한 수에 3~10초
- **작업량이 지금까지 만든 것 전부보다 큼.** 망 변환이 제일 까다로움

사용자에게 이미 설명했고, "며칠짜리 작업"이라고 안내했습니다.
아직 하겠다는 결정은 안 났습니다.

---

## 반드시 지켜야 할 것 (여기서 여러 번 사고 남)

### 1. 배치 파일은 CP949 로 저장

UTF-8 로 저장하면 한국어 Windows 의 cmd 가 한글 줄을 깨뜨려서
`'KataGo'은(는) 내부 또는 외부 명령...` 같은 에러가 쏟아집니다.

```python
open("파일.bat", "w", encoding="cp949", newline="\r\n")
```

### 2. PowerShell 스크립트는 UTF-8 BOM

```python
open("파일.ps1", "w", encoding="utf-8-sig", newline="\r\n")
```

### 3. 설치 경로를 마음대로 정하지 말 것

사용자가 명시적으로 지적한 부분입니다. 위치를 정해야 하면 **물어볼 것.**

### 4. 사용자는 명령줄에 익숙하지 않음

터미널 명령을 나열하지 말고 **더블클릭으로 되는 .bat** 을 만들어 줄 것.
그리고 **파일이 실제로 그 폴더에 있는지 확인하고 안내할 것**
(`git pull` 전에는 새 .bat 이 없습니다).

### 5. 호칭

"아저씨" 같은 호칭 쓰지 말 것. 그냥 존댓말만.

---

## 더블클릭용 파일들

| 파일 | 하는 일 |
| --- | --- |
| `상태 확인.bat` | 파이썬·KataGo 가 잡히는지 확인. **문제 생기면 이것부터** |
| `KataGo 설치.bat` | KataGo 자동 설치. 경로 인자 가능 |
| `♟_바둑 두기.bat` | 바둑판 열기 |
| `갱신.bat` | `git pull` |
| `설치.bat` | 저장소 새로 받기 |

---

## 정리 안 된 것 (급하지 않음)

- `musicits/photo-cull` 의 `claude/baduk-game-dev-pibjo3` 브랜치가 남아 있음.
  코드는 전부 `go-baduk` 으로 옮겼으므로 지워도 됨.
  (auto 모드가 원격 브랜치 삭제를 막아서 못 지웠음. GitHub 웹에서 휴지통 클릭)
- `D:\Claude Code (2026)\6. 바둑` 안에 중복된 `6. 바둑` 폴더가 있었음.
  지웠는지 확인 필요.
- `0_문서색인.md` 에 이 프로젝트를 등록해야 함. 그 파일 형식을 못 봐서
  아직 못 씀. 내용을 받으면 같은 형식으로 항목을 써 줄 것.

---

## 개발 메모

- 규칙 엔진이 **파이썬과 자바스크립트 두 벌**입니다.
  동작이 어긋나면 **`baduk/board.py` 가 기준**이고, 테스트도 그쪽에 있습니다.
- `docs/bot.js` 는 속도 때문에 `rules.js` 와 따로 구현했습니다.
  타입 배열만 쓰고 탐색 중에 객체를 만들지 않습니다.
- 테스트: `pip install pytest && python -m pytest` (98개)
- 웹 배포는 main 에 push 하면 자동입니다 (약 20초).
